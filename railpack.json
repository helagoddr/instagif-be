{
  "$schema": "https://schema.railpack.com",
  "buildAptPackages": ["ffmpeg", "nodejs", "npm"],
  "steps": {
    "install": {
      "commands": [
        "npm --prefix backend install --no-audit --no-fund || npm install --no-audit --no-fund",
        "python -m pip install --upgrade pip",
        "python -m pip install --no-cache-dir -r backend/requirements.txt || python -m pip install --no-cache-dir -r requirements.txt"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm --prefix backend start || npm start"
  }
}
